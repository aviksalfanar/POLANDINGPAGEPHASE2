<core:FragmentDefinition height="100%" xmlns:core="sap.ui.core" xmlns="sap.m" xmlns:mc="sap.suite.ui.microchart" xmlns:f="sap.f" xmlns:layout="sap.ui.layout" xmlns:card="sap.f.cards">
    <FlexBox justifyContent="Start" wrap="Wrap">
        <!-- Material Price Vs Old Price Card -->
        <VBox class="sapUiMediumMarginEnd sapUiSmallMarginBottom">
        <f:Card width="100%" height="100%">
            <f:header>
                <card:Header title="{i18n>MatPriceVsOldPrice}" />
            </f:header>
            <f:content>
                <FlexBox direction="Column" alignItems="Start">
                    <mc:RadialMicroChart
                        size="M"
                        percentage="{path: 'appModel>/MatPriceVsOldPriceMicro/Percentage', formatter: '.formatter.getPercentageForMicroChart'}"
                        alignContent="Center"
                        valueColor="{path: 'appModel>/MatPriceVsOldPriceMicro/Percentage', formatter: '.formatter.getMicroChartValueColor'}"
                    />
                    <FlexBox direction="Row" width="100%" justifyContent="SpaceBetween" class="sapUiContentPadding">
                        <FlexBox direction="Column" alignItems="Center">
                            <Text text="Match" />
                            <Text text="{appModel>/MatPriceVsOldPriceMicro/YMatch}" />
                        </FlexBox>
                        <FlexBox direction="Column" alignItems="Center">
                            <Link text="Non-Match" press="onMatPriceVsOldPrice" emphasized="true" textAlign="End" enabled="{= ${appModel>/MatPriceVsOldPriceMicro/NMatch} > 0 ? true : false}" />
                            <Text text="{appModel>/MatPriceVsOldPriceMicro/NMatch}" />
                        </FlexBox>
                    </FlexBox>
                </FlexBox>
            </f:content>
        </f:Card>
        </VBox>

        <!-- Materials UOM -->
        <VBox class="sapUiMediumMarginEnd sapUiSmallMarginBottom">
        <f:Card width="110%" height="100%">
            <f:header>
                <card:Header title="{i18n>MaterialsUom}" />
            </f:header>
            <f:content>
                <FlexBox direction="Column" alignItems="Start">
                    <mc:RadialMicroChart size="M" percentage="{path: 'appModel>/MaterialUomMicro/Percentage', formatter: '.formatter.getPercentageForMicroChart'}" alignContent="Center" valueColor="{path: 'appModel>/MaterialUomMicro/Percentage', formatter: '.formatter.getMicroChartValueColor'}" />
                    <FlexBox direction="Row" width="100%" justifyContent="SpaceBetween" class="sapUiContentPadding">
                        <FlexBox direction="Column" alignItems="Center">
                            <Text text="Match" />
                            <Text text="{appModel>/MaterialUomMicro/YMatch}" />
                        </FlexBox>
                        <FlexBox direction="Column" alignItems="Center">
                            <Link text="Non-Match" press="onMaterialUom" emphasized="true" textAlign="End" enabled="{= ${appModel>/MaterialUomMicro/NMatch} > 0 ? true : false}" />
                            <Text text="{appModel>/MaterialUomMicro/NMatch}" />
                        </FlexBox>
                    </FlexBox>
                </FlexBox>
            </f:content>
        </f:Card>
        </VBox>

        <!-- Order Currency -->
        <VBox class="sapUiMediumMarginEnd sapUiSmallMarginBottom">
        <f:Card width="110%" height="100%">
            <f:header>
                <card:Header title="{i18n>OrderCurrency}" />
            </f:header>
            <f:content>
                <FlexBox direction="Column" alignItems="Start">
                    <mc:RadialMicroChart size="M" percentage="{path: 'appModel>/OrderCurrencyMicro/Percentage', formatter: '.formatter.getPercentageForMicroChart'}" alignContent="Center" valueColor="{path: 'appModel>/OrderCurrencyMicro/Percentage', formatter: '.formatter.getMicroChartValueColor'}" />
                    <FlexBox direction="Row" width="100%" justifyContent="SpaceBetween" class="sapUiContentPadding">
                        <FlexBox direction="Column" alignItems="Center">
                            <Text text="Match" />
                            <Text text="{appModel>/OrderCurrencyMicro/YMatch}" />
                        </FlexBox>
                        <FlexBox direction="Column" alignItems="Center">
                            <Link text="Non-Match" press="onOrderCurrency" emphasized="true" textAlign="End" enabled="{= ${appModel>/OrderCurrencyMicro/NMatch} > 0 ? true : false}"/>
                            <Text text="{appModel>/OrderCurrencyMicro/NMatch}" />
                        </FlexBox>
                    </FlexBox>
                </FlexBox>
            </f:content>
        </f:Card>
        </VBox>

        <!-- PO Type -->
        <VBox class="sapUiMediumMarginEnd sapUiSmallMarginBottom">
        <f:Card width="110%" height="100%">
            <f:header>
                <card:Header title="{i18n>POType}" />
            </f:header>
            <f:content>
                <FlexBox direction="Column" alignItems="Start">
                    <mc:RadialMicroChart size="M" percentage="{path: 'appModel>/POTypeMicro/Percentage', formatter: '.formatter.getPercentageForMicroChart'}" alignContent="Center" valueColor="{path: 'appModel>/POTypeMicro/Percentage', formatter: '.formatter.getMicroChartValueColor'}" />
                    <FlexBox direction="Row" width="100%" justifyContent="SpaceBetween" class="sapUiContentPadding">
                        <FlexBox direction="Column" alignItems="Center">
                            <Text text="Match" />
                            <Text text="{appModel>/POTypeMicro/YMatch}" />
                        </FlexBox>
                        <FlexBox direction="Column" alignItems="Center">
                            <Link press="onPOType" text="Non-Match" emphasized="true" textAlign="End" enabled="{= ${appModel>/POTypeMicro/NMatch} > 0 ? true : false}"/>
                            <Text text="{appModel>/POTypeMicro/NMatch}" />
                        </FlexBox>
                    </FlexBox>
                </FlexBox>
            </f:content>
        </f:Card>
        </VBox>

        <!-- Payment Terms -->
        <VBox class="sapUiMediumMarginEnd sapUiSmallMarginBottom">
        <f:Card width="113%" height="100%">
            <f:header>
                <card:Header title="{i18n>PaymentTerms}" />
            </f:header>
            <f:content>
                <FlexBox direction="Column" alignItems="Start">
                    <mc:RadialMicroChart size="M" percentage="{path: 'appModel>/PaymentTermsMicro/Percentage', formatter: '.formatter.getPercentageForMicroChart'}" alignContent="Center" valueColor="{path: 'appModel>/PaymentTermsMicro/Percentage', formatter: '.formatter.getMicroChartValueColor'}" />
                    <FlexBox direction="Row" width="100%" justifyContent="SpaceBetween" class="sapUiContentPadding">
                        <FlexBox direction="Column" alignItems="Center">
                            <Text text="Match" />
                            <Text text="{appModel>/PaymentTermsMicro/YMatch}" />
                        </FlexBox>
                        <FlexBox direction="Column" alignItems="Center">
                            <Link press="onPaymentTerms" text="Non-Match" emphasized="true" textAlign="End" enabled="{= ${appModel>/PaymentTermsMicro/NMatch} > 0 ? true : false}"/>
                            <Text text="{appModel>/PaymentTermsMicro/NMatch}" />
                        </FlexBox>
                    </FlexBox>
                </FlexBox>
            </f:content>
        </f:Card>
        </VBox>

        <!-- Inventory Analysis -->
        <VBox class="sapUiMediumMarginEnd">
        <f:Card width="113%" height="100%">
            <f:header>
                <card:Header title="{i18n>InventoryAnalysis}" />
            </f:header>
            <f:content>
                <FlexBox direction="Column" alignItems="Start">
                    <mc:RadialMicroChart size="M" percentage="{path: 'appModel>/InventoryAnalysisMicro/Percentage', formatter: '.formatter.getPercentageForMicroChart'}" alignContent="Center" valueColor="{path: 'appModel>/InventoryAnalysisMicro/Percentage', formatter: '.formatter.getMicroChartValueColor'}" />
                    <FlexBox direction="Row" width="100%" justifyContent="SpaceBetween" class="sapUiContentPadding">
                        <FlexBox direction="Column" alignItems="Center">
                            <Text text="Match" />
                            <Text text="{appModel>/InventoryAnalysisMicro/YMatch}" />
                        </FlexBox>
                        <FlexBox direction="Column" alignItems="Center">
                            <Link press="onInventoryAnalysis" text="Non-Match" emphasized="true" textAlign="End" enabled="{= ${appModel>/InventoryAnalysisMicro/NMatch} > 0 ? true : false}"/>
                            <Text text="{appModel>/InventoryAnalysisMicro/NMatch}" />
                        </FlexBox>
                    </FlexBox>
                </FlexBox>
            </f:content>
        </f:Card>
        </VBox>

        <!-- Open NCR VS Vendor -->
        <VBox class="sapUiMediumMarginEnd">
        <f:Card width="105%" height="100%">
            <f:header>
                <card:Header title="{i18n>OpenNCRVsVendor}" />
            </f:header>
            <f:content>
                <FlexBox direction="Column" alignItems="Start">
                    <mc:RadialMicroChart size="M" percentage="{path: 'appModel>/OpenNcrVsVendor/Percentage', formatter: '.formatter.getPercentageForMicroChart'}" alignContent="Center" valueColor="{path: 'appModel>/OpenNcrVsVendor/Percentage', formatter: '.formatter.getMicroChartValueColor'}" />
                    <FlexBox direction="Row" width="100%" justifyContent="SpaceBetween" class="sapUiContentPadding">
                        <FlexBox direction="Column" alignItems="Center">
                            <Text text="Match" />
                            <Text text="{appModel>/OpenNcrVsVendor/YMatch}" />
                        </FlexBox>
                        <FlexBox direction="Column" alignItems="Center">
                            <Link press="onOpenNcrVsVendor" text="Non-Match" emphasized="true" textAlign="End" enabled="{= ${appModel>/OpenNcrVsVendor/NMatch} > 0 ? true : false}"/>
                            <Text text="{appModel>/OpenNcrVsVendor/NMatch}" />
                        </FlexBox>
                    </FlexBox>
                </FlexBox>
            </f:content>
        </f:Card>
        </VBox>
    </FlexBox>
</core:FragmentDefinition>
